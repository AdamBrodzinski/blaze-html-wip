# TODO

- optimize finding tags with component name first, eg search for 'E' and then check if preceeding char is '<'
- have template engine keep track of index for starting tags for Each, then skip to that point for the nth iteration
